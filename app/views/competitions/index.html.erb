<h2>All Competitions 🏆</h2>
<% @competitions.each do |competition| %>
    <p><%= link_to competition.name, competition %></p>
  <% end %>
  <button type="button" class="btn btn-light"><%= link_to "Create a Competition", new_competition_path, :class=>"text-success" %></button>
  